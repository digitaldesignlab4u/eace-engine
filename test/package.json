{
  "name": "eace-tasting-menu-regression",
  "version": "1.0.0",
  "description": "Regression harness for eace-compliance-tasting-menu.html — drives every mode x track x sector combination through a full quiz session and checks for zero console errors.",
  "private": true,
  "scripts": {
    "test": "node run.js"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
